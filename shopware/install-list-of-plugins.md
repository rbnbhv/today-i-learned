# HowTo install list of store.shopware.com plugins 

- Add all the `store.shopware.com/plugins` to the `composer.json`
- Run the command `composer update require/store.shopware.com/*` or `composer update require/*`
