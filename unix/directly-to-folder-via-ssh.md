# If a desired folder is to be opened, the following parameter can be passed:

```RequestTTY yes```

```RemoteCommand cd html/shopware/ && $SHELL -i```

- If the command "RequstTTY yes" is not passed, the message: no job control can appear.
